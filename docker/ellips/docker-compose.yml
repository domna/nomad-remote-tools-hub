version: "3.9"

services:
  ellips:
    build: 
      context: ./
      dockerfile: Dockerfile
    ports:
      - 8888:8888
    volumes:
      - ./example:/home/jovyan/work
    working_dir: /home/jovyan/work